import { DeliverSection, DetailsBox, PopSection, Questions, SecondContainer } from '@components/HomeItems';

export const HomeWrapper = () => {
	return (
		<div>
			<DetailsBox />
			<SecondContainer />
			<PopSection />
			<DeliverSection />
			<Questions />
		</div>
	);
};
