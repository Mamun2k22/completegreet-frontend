// const fs = require('fs');
// const isoCountries = require('i18n-iso-countries');

// isoCountries.registerLocale(require('i18n-iso-countries/langs/en.json'));

// const countryCodes = isoCountries.getNames('en');

// const countries = Object.keys(countryCodes).map((code) => {
//   const name = countryCodes[code];
//   const short = isoCountries.alpha2ToAlpha3(code.substr(0, 2));
//   return { name, short, code };
// });

// fs.writeFileSync('countries.json', JSON.stringify(countries, null, 2));

// module.exports = countries;
